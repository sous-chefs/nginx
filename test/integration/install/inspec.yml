---
name: install
title: Integration tests for nginx_install
summary: This InSpec profile contains integration tests for nginx cookbook
version: 0.1.0
supports:
  - os-family: linux
inputs:
- name: nginx_package_name
  description: The name of the nginx package, for example "nginx", "nginx-extras", "nginx-full".
  type: string
  value: nginx

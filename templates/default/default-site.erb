server {
  listen       <%= @port -%>;
  server_name  <%= @server_name %>;

  access_log   <%= @nginx_log_dir %>/localhost.access.log;

  location / {
    root   <%= @default_root %>;
    index  index.html index.htm;
  }
}

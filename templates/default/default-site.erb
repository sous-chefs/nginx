# Generated by Chef for <%= node['fqdn'] %>
# Do NOT modify this file by hand.
#

server {
  listen       <%= @port -%>;
  server_name  <%= @server_name %>;

  access_log   <%= @nginx_log_dir %>/localhost.access.log;
  error_log    <%= @nginx_log_dir %>/localhost.error.log;

  location / {
    root       <%= @default_root %>;
    index      index.html index.htm;
  }
}

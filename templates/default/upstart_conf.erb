# nginx
 
description "nginx http daemon"
author "Tom Taylor <tom@newspaperclub.com>"
 
start on (filesystem and net-device-up IFACE=lo)
stop on runlevel [!2345]
 
console output

exec <%= node['nginx']['binary'] %> -c /etc/nginx/nginx.conf  -g "daemon off;"

respawn
